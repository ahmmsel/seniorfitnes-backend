# ==============================================
# BROADCASTING CONFIGURATION FOR PUSHER
# ==============================================

# Set the broadcast driver to pusher
BROADCAST_DRIVER=pusher

# Pusher Credentials
# Get these from: https://dashboard.pusher.com/
PUSHER_APP_ID=your-app-id
PUSHER_APP_KEY=your-app-key
PUSHER_APP_SECRET=your-app-secret
PUSHER_APP_CLUSTER=your-cluster  # e.g., eu, us2, ap1

# Optional: Enable encrypted connections (recommended for production)
PUSHER_SCHEME=https
PUSHER_PORT=443

# ==============================================
# USAGE INSTRUCTIONS
# ==============================================
# 1. Copy these values to your .env file
# 2. Replace with your actual Pusher credentials
# 3. Restart your Laravel application
# 4. Test with: php artisan tinker
#    broadcast(new App\Events\MessageSent($message));
